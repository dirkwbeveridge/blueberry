---
context: default
phase: push-notifications
status: in_progress
last_updated: 2026-06-20T12:00:00-07:00
---

# Continue Here

Work is in the real repo on branch `main` at `/Users/dbeveridge/Projects/Blueberry`.

<current_state>
The APNs/iOS notification implementation has been reviewed and hardened in the real repo.
The recent fixes corrected timezone-unsafe appointment storage, moved local reminder IDs
out of shared appointment rows and into device-local storage, aligned the push-token
migration with the consolidated schema, and documented the APNs Edge Function setup.
</current_state>

<verified>
- `npm run typecheck` passes.
- `npm run lint` passes.
- `git diff --check` passes.
- The iOS workspace build succeeded for `ios/Blueberry.xcworkspace`.
</verified>

<remaining_work>
- Deploy or confirm `supabase-schema.sql` plus `supabase/migrations/20260620100000_create_device_push_tokens.sql` in the active Supabase project.
- Add APNs function secrets and deploy `supabase/functions/send-apns-notification`.
- Verify the Notifications modal and APNs token registration on a real iPhone.
- Send a real APNs test against a stored device token.
- After push verification, move to calendar sync and Family Mode execution.
</remaining_work>

<blockers>
- Supabase dashboard/project access is required for schema deployment, secrets, and function deployment.
- A real iPhone is required for APNs token registration and end-to-end delivery verification.
- Apple APNs key material (`APNS_TEAM_ID`, `APNS_KEY_ID`, `APNS_PRIVATE_KEY`) is still external.
</blockers>

<next_action>
Start with external-state verification: deploy the backend pieces and test on a physical iPhone.
Local repo work is now in a build-verified state.
</next_action>
