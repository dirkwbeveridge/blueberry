---
context: default
phase: multi-workstream
task: null
total_tasks: null
status: at_execution_boundary
last_updated: 2026-06-09T12:59:08Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was locked by explicit user decision.
> Acknowledge each one before touching any planning or code.

- [ ] **PUSH-TECH-STACK** — Push notification plan `01-01-PLAN.md` was written for `expo-notifications` + EAS, but the user explicitly said "I don't want to use Expo" for push. The plan **must be rewritten** for native APNs/FCM dispatched from Supabase Edge Functions before execution begins.
- [ ] **HEALTH-DATA-PRIVACY** — Push payloads must never carry raw health-log data. Enforced server-side by `assertNoHealthData()` in `supabase/functions/shared/pushPayload.ts`. This is a hard product rule, not a suggestion.
- [ ] **NO-HARDCODED-DUE-DATE** — Never seed `2025-10-11` (or any hardcoded due date) anywhere in code. Due date is always user-entered, sourced from `households.due_date`.
- [ ] **NO-EAS** — No Expo Application Services. Build and push infra stays native (APNs/FCM) with Supabase Edge Functions as the dispatch layer.
- [ ] **REQUIREMENTS-NOT-UPDATED** — REQUIREMENTS.md still lists INTG-01 through INTG-04 as "Candidate v1.5/v2". User decided these are v1 scope. Update before the next planning session, or a resuming agent will plan against the wrong scope.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| expo-push assumption | 01-01-PLAN.md was generated before the "no Expo push" decision. Any agent reading that plan will adopt the wrong tech stack. | blocking | Rewrite 01-01-PLAN.md for native APNs/FCM before any push execution begins |
| Stale REQUIREMENTS.md | INTG-01-04 are listed as deferred but are v1 scope. Planning agents seeded from REQUIREMENTS.md will build the wrong scope. | blocking | Update REQUIREMENTS.md before next planning session |
| Hardcoded due date | Test household is already postpartum (due 2025-10-11). Never use this date in fixtures. | blocking | Always read due_date from households table or user-entered value |

<current_state>
All 4 workstreams are in "Planned" state — PLAN.md files written, no execution started.
Last commit: `6520e00` — "Stand up 3 workstreams with parallel phase plans + feature mockups"
Git is clean except 4 untracked placeholder files (journal.tsx, partner.tsx, index.tsx, PlaceholderScreen.tsx).
Active workstream (`.planning/active-workstream`): family-mode
</current_state>

<completed_work>
This session (2026-06-09):
- Created 4 workstream scaffolds with STATE.md files (core-app, calendar-sync, push-notifications, family-mode)
- calendar-sync PLAN.md — 11 tasks covering sub-phases 7a (Google Calendar OAuth PKCE) and 7b (Apple EventKit)
- push-notifications 01-01-PLAN.md — 3 tasks, 1 wave; includes assertNoHealthData() privacy guard design
- family-mode PLAN.md — 4 tasks covering stage transition, postpartum UI, night-shift swap, recovery check-in
- docs/feature-mockups.html — 7 iPhone screens (Mom pregnant, Mom postpartum night, Partner views, appointments, notifications, family-mode)
- Dark mode applied to both Mom and Partner postpartum screens at nighttime hours
- .claude/settings.json — read-only Bash permission allowlist
- .claude/launch.json — docs preview server on port 4321
- All committed: 6520e00 (11 files, 3,130 insertions)

Prior work (earlier sessions):
- Supabase schema deployed (8 tables, RLS, Realtime on todos/health_logs/journal_entries/appointments)
- Auth gate, role-aware tabs, partner-join RPC, SecureStore adapter, icons/splash — all committed
- docs/mockups.html, docs/blueberry-story.html, docs/voice-guide.html — design/voice artifacts locked
- HANDOFF.md, STATE.md, ROADMAP.md established
</completed_work>

<remaining_work>
**Immediate (before next execution):**
1. Update `REQUIREMENTS.md` — move INTG-01, INTG-02, INTG-04 to v1 scope (INTG-03 AI content remains deferred per ROADMAP Phase 7d). Remove from "Candidate v1.5/v2" section.
2. Rewrite `push-notifications/phases/01-push-notifications/01-01-PLAN.md` — replace expo-notifications/EAS with native APNs/FCM via Supabase Edge Functions.

**User actions required before any workstream can execute:**
- Deploy `supabase-schema.sql` then `supabase_migration.sql` in Supabase SQL Editor at `fpfncmrkptwvmnaqdixx.supabase.co` — **blocks core-app entirely**
- Create Google Cloud Console project, OAuth 2.0 client, enable Calendar API — **blocks calendar-sync**

**Execution order (once Supabase deployed):**
1. core-app Phases 1–2 already scaffolded; Phase 3–6 planning needed
2. family-mode — runs independently, can execute while 7a/7b/7c are in flight
3. calendar-sync — depends on core-app Phase 6 (appointments) being live
4. push-notifications — can start after core-app Phase 1 (push token registration)

**Uncommitted placeholder files (minor):**
- app/(tabs)/journal.tsx
- app/(tabs)/partner.tsx
- app/index.tsx
- components/shared/PlaceholderScreen.tsx
</remaining_work>

<decisions_made>
- INTG-01 through INTG-04 are v1 scope (user: "must be built in this first product launch")
- Push notifications: native APNs/FCM via Supabase Edge Functions — NOT expo-notifications, NOT EAS
- Push payloads: no raw health-log data; only shared_note, appointment title, todo title, or generic signal
- Google OAuth tokens: stored per users.id, not household-shared
- appointments.notes: never synced to external calendars (privacy)
- Google Calendar conflict resolution: last-write-wins
- Family Mode trigger: More tab menu item "Begin Family Mode"
- Night-shift swap storage: household_events table (realtime, not Zustand-only)
- Postpartum content: checkpoint before shipping — user wants review before execution completes
- Dark mode: auto-activates at nighttime hours for both Mom and Partner roles
- No EAS (Expo Application Services) anywhere in the project
</decisions_made>

<blockers>
1. **Supabase schema not deployed** — golden path E2E cannot run; core-app execution blocked
   Action: SQL Editor at fpfncmrkptwvmnaqdixx.supabase.co → run supabase-schema.sql → run supabase_migration.sql
2. **Push plan uses wrong tech stack** — 01-01-PLAN.md written for expo-notifications/EAS
   Action: Rewrite plan for native APNs/FCM before execution
3. **Google Cloud Console project not created** — calendar-sync cannot be tested
   Action: Create project manually; set up OAuth client; enable Calendar v3 API
</blockers>

## Required Reading (in order)
1. `.planning/REQUIREMENTS.md` — scope truth; note INTG-01-04 must be moved to v1 before next plan
2. `.planning/ROADMAP.md` — phase structure; workstream-to-phase mapping
3. `.planning/workstreams/push-notifications/STATE.md` — privacy boundary decision (authoritative)
4. `.planning/workstreams/push-notifications/phases/01-push-notifications/01-01-PLAN.md` — **read with critical eye: entire push stack must be rewritten for APNs/FCM**
5. `.planning/workstreams/calendar-sync/phases/01-calendar-sync/PLAN.md` — 11-task plan; OQ-1 through OQ-4 are open
6. `.planning/workstreams/family-mode/phases/01-family-mode/PLAN.md` — 4-task plan; ready to execute
7. `CLAUDE.md` — project conventions, security constraints, architecture

## Infrastructure State
- Supabase project: `fpfncmrkptwvmnaqdixx.supabase.co` — **schema NOT yet deployed** (user action pending)
- Expo app: not currently running; start with `npx expo start --localhost --port 8081`
- Docs preview server: `python3 -m http.server 4321 --directory docs` (or via .claude/launch.json)
- No background processes running

## Open Questions in Active Plans

**calendar-sync:**
- OQ-1: Google OAuth client secret — Edge Function proxy vs. native iOS public client
- OQ-2: Google Cloud Console project (requires manual creation)
- OQ-3: edit-appointment.tsx — new modal or param-driven mode of add-appointment.tsx?
- OQ-4: Answered: last-write-wins for conflict resolution

**push-notifications:**
- Full plan rewrite needed: remove expo-notifications, add APNs/FCM via Edge Functions
- pg_cron and DB webhook setup docs need to land in SUPABASE-SETUP.md

<context>
Session was focused on planning, not execution. Three parallel planning agents produced three detailed PLAN.md files simultaneously. The key unresolved risk going into the next session is the push notification tech stack mismatch — the plan says expo-notifications but the user wants native APNs/FCM. That rewrite should be the first thing done before touching any push code. The second priority is updating REQUIREMENTS.md so INTG-01-04 are correctly scoped as v1.

Family mode is the most self-contained workstream and the most urgent relative to the test household's real-world situation (34 weeks postpartum as of today). It can execute independently once the Supabase schema is deployed.
</context>

<next_action>
Start here:
1. Update REQUIREMENTS.md — move INTG-01, INTG-02, INTG-04 checkboxes from "Candidate v1.5/v2" into v1 scope. Mark INTG-03 (AI content) as still deferred per Phase 7d.
2. Rewrite .planning/workstreams/push-notifications/phases/01-push-notifications/01-01-PLAN.md — replace expo-notifications + EAS with native APNs/FCM approach. APNs auth via p8 key stored as Supabase secret; FCM via service account JSON; dispatch from Edge Functions.
3. Commit both changes.
Then, if the user has deployed the Supabase schema: kick off core-app workstream Phase 3-6 planning (or begin executing family-mode, which is ready to go).
</next_action>
