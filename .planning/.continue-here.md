---
context: default
phase: design-system-foundation (not a formal GSD phase — git branch)
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-06-11T06:49:16.017Z
---

# Continue Here — Design System Foundation

Work is on git branch **`design-system-foundation`** (3 commits ahead of `main`, tree clean).
This is a pre-phase consolidation, not a tracked GSD phase, so the handoff lives at the default `.planning/` path.

<current_state>
Consolidating the design system before building remaining feature phases. Tasks 1-2 of 4 are
committed and automated-verified (typecheck clean, E2E 20/20). Visual device verification of the
refactored modals is the pending gate before Task 3.
</current_state>

<completed_work>
- Task 1 (`cb5bdaf`): Added 7-step `typography` scale + tint colors (`primaryTint/successTint/warningTint/errorTint/shadow`) to `constants/theme.ts`; wired `Badge` to the new tokens. Done.
- Task 2 (`7bd944c`): Extracted `DateField`, `TimeField`, `Chip` into `components/ui/`; refactored `add-todo`, `add-appointment`, `log-symptom` to use them; fixed PS-41 (Save enables when only notes entered). Done.
- (`9b844e2`): Logged PS-60 (mood selector went from tiles to horizontal pills).
</completed_work>

<remaining_work>
- Task 3 (not started): Extract layout primitives — `ModalSheet` (all modals), `ScreenHeader` (all tabs), `EmptyState` (todo/health/calendar), `SegmentedControl` (todo tabs + calendar toggle + login toggles).
- Task 4 (not started): Token sweep — replace hardcoded hex / `#FFFFFF` / `fontSize` literals app-wide with tokens + the typography scale; reconcile `week-detail.tsx` off-brand palette (SYS-04); fix PS-53 (contraction timer can save a corrupt mid-contraction record) and PS-01 (login due-date should use `DateField`).
</remaining_work>

<decisions_made>
- Foundation-first over parallel phase builds — parallel builders would hardcode against drifting tokens and collide on shared files. (User chose this.)
- Foundation tasks run sequentially (all touch `theme.ts` / `components/ui`); parallelism is reserved for the feature phases afterward.
- Chip currently renders horizontally (emoji left of label). Open question: keep it, or add a vertical variant to restore the mood-grid tile look (PS-60).
</decisions_made>

<blockers>
- Visual verification pending: typecheck/E2E can't see layout. Run `npx expo start --port 8081` on this branch (Expo Go works — the native-build switch is only on `main`) and check add-todo, add-appointment, log-symptom.
</blockers>

## Required Reading (in order)
1. `docs/design-system.md` — the spec driving Tasks 1-4 (token catalog, type scale, primitives to extract).
2. `docs/ui-backlog.md` (on this branch) — SYS-01..08 systemic items + per-screen findings.
3. `.planning/HANDOFF.json` — machine-readable state.

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Dispatching parallel implementer agents on foundation tasks → they collide on `theme.ts`/`components/ui`. Mitigation: run foundation tasks sequentially with review gates.
- [ANTI-PATTERN]: Presenting a refactor as "verified" on typecheck alone → it can't catch layout/behavior regressions. Mitigation: device verification is a required gate before merge.

## Infrastructure State
- Branch `design-system-foundation`, tree clean. `main` has 3 newer commits (native-build switch, E2E expansion, second UI audit) NOT on this branch.
- MERGE HAZARD: `docs/ui-backlog.md` diverged — `main` has PS-60..PS-85 (incl. 3 P1s: PS-61 setup-step unreachable, contraction-timer invisible "Stop", journal tab unreachable); this branch has a different PS-60 (mood chip). Reconcile PS numbering on merge.
- `.planning/STATE.md` and `ROADMAP.md` are stale (2026-05-23) — predate all auth + foundation work. Refresh before using `gsd-manager`.

<context>
The plan: land the design-system foundation (4 sequential tasks via subagent-driven-development with
spec + quality review per task), then build the remaining feature phases (calendar, push, family-mode,
Journal screen) in parallel git worktrees on the stable foundation. Tasks 1-2 done; awaiting the user's
device check + mood-chip decision before Task 3.
</context>

<next_action>
Start with: get the user's call on the mood-chip layout (PS-60 — keep horizontal vs add vertical Chip
variant), then dispatch the Task 3 implementer (layout primitives) using subagent-driven-development.
</next_action>
